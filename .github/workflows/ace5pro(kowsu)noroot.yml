name: ace5pro(kowsu)noroot
on:
  workflow_dispatch:
    inputs:
      CPU:
        description: "分支"
        required: true
        default: 'sm8750'
      FEIL:
        description: "配置文件"
        required: true
        default: 'oneplus_ace5_pro-112'
      ANDROID_VERSION:
        description: "内核安卓版本"
        required: true
        default: 'android15'
      KERNEL_VERSION:
        description: "内核版本"
        required: true
        default: '6.6'
      KERNEL_NAME:
        description: "修改内核名称"
        required: true
        default: '-android15-8-g29d86c5fc9dd-abogki428889875'
      local_version:
        description: '输入内核后缀名（如-v8）'
        required: false
        default: '-4k'
        type: string
      remove_default_4k:
        description: '是否删除默认的 -4k 后缀？（勾选则删除）'
        required: false
        default: false
        type: boolean
      kernel_time:
        description: '内核构建时间（UTC 时间字符串，例如 "Mon May 12 09:09:59 UTC 2025"）'
        required: false
        default: 'Tue Jul  1 19:48:18 UTC 2025'
        type: string
      
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      TOKEN: ${{ secrets.TOKEN }}
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          
      - name: Configure Git
        run: |
         git config --global user.name "build"
         git config --global user.email "3620603668@qq.com"

      # 1. 拉取仓库  
      - name: Checkout  
        uses: actions/checkout@v4  

      # 2. 安装构建依赖（包括 ccache）  
      - name: Install Dependencies  
        run: |  
         sudo apt-get update  
         sudo apt-get install -y python3 git curl ccache libelf-dev
         sudo apt-get install -y dos2unix  

      # 3. 恢复 ccache 缓存  
      - name: Restore ccache
        uses: actions/cache@v3
        with:
         path: /home/runner/.ccache
         key: ${{ runner.os }}-${{ github.repository }}-v2_noroot-112
         restore-keys: |
          ${{ runner.os }}-${{ github.repository }}-
          ${{ runner.os }}-
          ccache-

      # 4. 设置 ccache 环境变量  
      - name: Setup ccache environment  
        run: |  
         echo "CCACHE_DIR=/home/runner/.ccache" >> $GITHUB_ENV  
         echo "CCACHE_MAXSIZE=8G" >> $GITHUB_ENV  
         echo "CCACHE_COMPILERCHECK=%compiler% -dumpmachine; %compiler% -dumpversion" >> $GITHUB_ENV  
         echo "CCACHE_NOHASHDIR=true" >> $GITHUB_ENV  
         echo "CCACHE_HARDLINK=true" >> $GITHUB_ENV  
         echo "CCACHE_BASEDIR=${{ github.workspace }}" >> $GITHUB_ENV  
         echo "CCACHE_LOGFILE=${{ github.workspace }}/ccache.log" >> $GITHUB_ENV  
         echo "/usr/lib/ccache" >> $GITHUB_PATH  
         
      - name: Show ccache stats
        run: |
         ccache -s || true

      - name: Install repo tool
        run: |
         curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
         chmod a+x ~/repo
         sudo mv ~/repo /usr/local/bin/repo

      - name: Initialize repo and sync
        run: |
         mkdir kernel_workspace && cd kernel_workspace
         repo init -u https://github.com/3620603660/kernel_manifest.git -b refs/heads/oneplus/${{ github.event.inputs.CPU }} -m ${{ github.event.inputs.FEIL }}.xml --depth=1
         repo --trace sync -c -j$(nproc --all) --no-tags
         rm kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
         rm kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"

      - name: Export suffix
        run: |
          echo "TAG_SUFFIX=${{ github.event.inputs.KernelSU_Nex_tag }}" >> $GITHUB_ENV

      - name: 设置内核优化配置
        run: |
          cd kernel_workspace/kernel_platform
          CONFIG_FILE=./common/arch/arm64/configs/gki_defconfig
          
          # 向 gki_defconfig 中追加配置 (精简、优化并专业化)
          cat <<EOF >> ./common/arch/arm64/configs/gki_defconfig
          
          # =================================================================
          # Kernel Configuration Optimizations (ace5pro - Merged & Refined)
          # =================================================================
          
          # -----------------------------------------------------------------
          # 1. Compiler and Build Optimizations
          # -----------------------------------------------------------------
          CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y   # 使用 -O2 优化级
          CONFIG_LOCALVERSION_AUTO=n             # 禁用自动附加 Git commit ID
          CONFIG_LTO_CLANG=y                     # 启用 Clang 链接时优化 (LTO)
          CONFIG_LTO_CLANG_THIN=y                # 启用 Thin LTO (加速编译)
          CONFIG_LTO_POLLY=y                     # 启用 Polly 优化器 (循环优化)
          
          # -----------------------------------------------------------------
          # 2. CPU Scheduling and Power Management
          # -----------------------------------------------------------------
          CONFIG_CPU_FREQ=y                      # CPU 频率调节支持
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y        # Schedutil 调速器 (基于利用率的现代调速器)
          CONFIG_CPU_HOTPLUG=y                   # CPU 热插拔支持
          CONFIG_CPU_IDLE=y                      # CPU 空闲状态管理
          CONFIG_CPU_IDLE_GOV_MENU=y             # Menu CPU Idle 调速器 (节能)
          CONFIG_CPU_IDLE_GOV_TEO=y              # TEO CPU Idle 调速器 (能耗感知)
          CONFIG_NO_HZ_IDLE=y                    # 空闲 CPU 动态时钟节拍 (提升节能)
          CONFIG_SCHED_AUTOGROUP=y               # 自动任务分组 (公平调度)
          CONFIG_SCHED_TUNE_BOOST=y              # 调度器调优提升 (性能优化)
          CONFIG_SCHED_CORE=y                    # 核心调度支持
          CONFIG_SCHED_EAS=y                     # 能源感知调度 (EAS)
          CONFIG_SCHED_HRTICK=y                  # 高精度调度时钟
          CONFIG_SCHED_MC=y                      # 多核调度
          CONFIG_SCHED_THERMAL_PRESSURE=y        # 调度器感知热压力
          CONFIG_SCHED_TUNE=y                    # 调度器调优框架
          CONFIG_SCHED_UTIL_CLAMP=y              # 调度利用率限制
          CONFIG_SCHED_WALT=y                    # WALT 负载跟踪
          CONFIG_UCLAMP_TASK=y                   # 任务级别利用率限制
          CONFIG_PM_AUTOSLEEP=y                  # 启用自动休眠
          CONFIG_PM_WAKELOCKS=y                  # 启用唤醒锁
          CONFIG_PM_WAKELOCKS_LIMIT=y            # 唤醒锁限制
          CONFIG_PM_WAKELOCKS_GC=y               # 唤醒锁垃圾回收
          CONFIG_PM_DOMAIN_CPU_GOV=y             # CPU 电源域调速器
          CONFIG_QCOM_CPU_CLUSTER_POWER_CONTROL=y# 高通 CPU 集群功耗控制
          CONFIG_THERMAL_GOV_FAIR_SHARE=y        # 公平分享散热调速器
          CONFIG_THERMAL_GOV_POWER_ALLOCATOR=y   # 电源分配散热调速器
          
          # -----------------------------------------------------------------
          # 3. Memory Management and ZRAM
          # -----------------------------------------------------------------
          CONFIG_ION=y                           # ION 内存管理器 (Android 必需)
          CONFIG_MEMCG=y                         # 内存控制组 (cgroups)
          CONFIG_MEMCG_SWAP=y                    # 内存控制组 Swap 支持
          CONFIG_ANDROID_LOW_MEMORY_KILLER=y     # Android 低内存杀手
          CONFIG_LRU_GEN=y                       # 多代 LRU 算法
          CONFIG_TRANSPARENT_HUGEPAGE=y          # 透明大页支持
          CONFIG_TRANSPARENT_HUGEPAGE_ALWAYS=n   # 不总是强制使用透明大页
          CONFIG_TRANSPARENT_HUGEPAGE_MADVISE=y  # 通过 madvise 选择性使用透明大页
          CONFIG_ZRAM=y                          # ZRAM 内存压缩
          CONFIG_ZRAM_LZ4=y                      # ZRAM 使用 LZ4 压缩
          CONFIG_ZRAM_ZSTD=y                     # ZRAM 使用 ZSTD 压缩
          CONFIG_ZRAM_MULTI_COMP=y               # 允许 ZRAM 使用多种压缩算法
          CONFIG_BLK_DEV_ZRAM_DISCARD=y          # ZRAM 处理 discard 请求
          CONFIG_ZSMALLOC=y                      # ZSMALLOC 内存分配器
          
          # -----------------------------------------------------------------
          # 4. I/O Schedulers and Storage
          # -----------------------------------------------------------------
          CONFIG_BLK_MQ=y                        # 多队列块层
          CONFIG_BLK_MQ_PCI=y                    # PCI 多队列块层
          CONFIG_BLK_WBT=y                       # 写回限速 (WBT)
          CONFIG_MQ_IOSCHED_KYBER=y              # Kyber I/O 调度器 (低延迟)
          CONFIG_MQ_IOSCHED_DEADLINE=y           # Deadline I/O 调度器
          CONFIG_MQ_IOSCHED_BFQ=y                # BFQ I/O 调度器 (公平性)
          CONFIG_BFQ_ALLOC_BATCH=y               # BFQ: 启用批量分配
          CONFIG_BFQ_GROUP_IOSCHED=y             # BFQ: 启用组 I/O 调度
          CONFIG_BLK_CGROUP_IOCOST=y             # 块 cgroup I/O 成本
          CONFIG_IO_URING=y                      # I/O Uring 框架
          CONFIG_IO_URING_ASYNC=y                # I/O Uring 异步支持
          CONFIG_PAGE_BOOST=y                    # 页面提升
          CONFIG_SWAP_ASYNC_IO=y                 # 异步 SWAP I/O
          # -----------------------------------------------------------------
          # 5. F2FS File System Optimizations
          # -----------------------------------------------------------------
          CONFIG_F2FS_FS_COMPRESSION=y           # F2FS 文件系统压缩
          CONFIG_F2FS_FS_LZORLE=y                # F2FS 的 LZORLE 压缩算法
          CONFIG_F2FS_IO_QUEUE_ENHANCEMENT=y     # F2FS I/O 队列增强
          CONFIG_F2FS_IO_LATENCY_ENHANCEMENT=y   # F2FS I/O 延迟增强
          CONFIG_F2FS_FIX_UNNECESSARY_DIO_SPLIT=y# 修复不必要的 DIO 分割
          CONFIG_F2FS_SUPPORT_NILFS_CHKALLOC=y   # NILFS 检查分配支持
          CONFIG_F2FS_CHECK_CHKSUM_TYPE=y        # F2FS 校验和类型检查
          CONFIG_F2FS_IOSTAT=y                   # F2FS I/O 统计
          CONFIG_F2FS_SUPPORT_PINNED_FILE=y      # F2FS 固定文件支持
          CONFIG_F2FS_SUPPORT_SSR=y              # F2FS SSR 支持
          CONFIG_F2FS_SUPPORT_VOLATILE_WRITE=y   # F2FS 易失性写入支持
          CONFIG_F2FS_FS_LZ4=y                   # LZ4 压缩算法
          CONFIG_F2FS_FS_ZSTD=y                  # ZSTD 压缩算法
          CONFIG_F2FS_ATOMIC_WRITE=y             # F2FS 原子写
          
          # -----------------------------------------------------------------
          # 6. Networking and TCP/IP
          # -----------------------------------------------------------------
          CONFIG_INET=y                          # TCP/IP 协议栈
          CONFIG_IPV6=y                          # IPv6 协议
          CONFIG_DEFAULT_TCP_CONG="bbr"          # 默认 TCP 拥塞控制为 BBR
          CONFIG_TCP_CONG_ADVANCED=y             # 高级 TCP 拥塞控制
          CONFIG_TCP_CONG_BBR=y                  # BBR 拥塞控制
          CONFIG_TCP_CONG_CUBIC=y                # CUBIC 拥塞控制
          CONFIG_TCP_CONG_HTCP=y                 # H-TCP 拥塞控制
          CONFIG_TCP_CONG_WESTWOOD=y             # Westwood 拥塞控制
          CONFIG_TCP_CONG_RENO=y                 # Reno 拥塞控制
          CONFIG_TCP_FASTOPEN=y                  # TCP Fast Open
          CONFIG_TCP_SACK=y                      # TCP SACK
          CONFIG_TCP_TIMESTAMPS=y                # TCP 时间戳
          CONFIG_BQL=y                           # Byte Queue Limits
          CONFIG_NET_SCHED=y                     # 网络调度器
          CONFIG_DEFAULT_NET_SCH="cake"          # 默认网络调度器为 CAKE
          CONFIG_NET_SCH_CAKE=y                  # CAKE 队列管理
          CONFIG_NET_SCH_FQ=y                    # 公平队列 (FQ) 调度器
          CONFIG_NET_SCH_FQ_CODEL=y              # FQ_CoDel 调度器
          CONFIG_BPF_STREAM_PARSER=y             # BPF 流解析器
          
          # -----------------------------------------------------------------
          # 7. VPN and Netfilter
          # -----------------------------------------------------------------
          CONFIG_WIREGUARD=y                     # WireGuard VPN 支持
          CONFIG_WIREGUARD_RING_SIZE=1024        # WireGuard 环形缓冲区大小
          CONFIG_IP6_NF_NAT=y                    # IPv6 NAT
          CONFIG_IP6_NF_TARGET_MASQUERADE=y      # IPv6 MASQUERADE
          CONFIG_IP_SET=y                        # IP Sets 框架
          CONFIG_IP_SET_BITMAP_IP=y              # IP Set: bitmap:ip
          CONFIG_IP_SET_BITMAP_IPMAC=y           # IP Set: bitmap:ip,mac
          CONFIG_IP_SET_BITMAP_PORT=y            # IP Set: bitmap:port
          CONFIG_IP_SET_HASH_IP=y                # IP Set: hash:ip
          CONFIG_IP_SET_HASH_IPMAC=y             # IP Set: hash:ip,mac
          CONFIG_IP_SET_HASH_IPMARK=y            # IP Set: hash:ip,mark
          CONFIG_IP_SET_HASH_IPPORT=y            # IP Set: hash:ip,port
          CONFIG_IP_SET_HASH_IPPORTIP=y          # IP Set: hash:ip,port,ip
          CONFIG_IP_SET_HASH_IPPORTNET=y         # IP Set: hash:ip,port,net
          CONFIG_IP_SET_HASH_MAC=y               # IP Set: hash:mac
          CONFIG_IP_SET_HASH_NET=y               # IP Set: hash:net
          CONFIG_IP_SET_HASH_NETIFACE=y          # IP Set: hash:net,iface
          CONFIG_IP_SET_HASH_NETNET=y            # IP Set: hash:net,net
          CONFIG_IP_SET_HASH_NETPORT=y           # IP Set: hash:net,port
          CONFIG_IP_SET_HASH_NETPORTNET=y        # IP Set: hash:net,port,net
          CONFIG_IP_SET_LIST_SET=y               # IP Set: list:set
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y   # Netfilter: addrtype 匹配
          CONFIG_NETFILTER_XT_SET=y              # Netfilter: set 匹配
          
          # -----------------------------------------------------------------
          # 8. Cryptography and Compression
          # -----------------------------------------------------------------
          CONFIG_CRYPTO_LZ4=y                    # LZ4 加密 API
          CONFIG_CRYPTO_LZ4HC=y                  # LZ4HC 加密 API
          CONFIG_CRYPTO_LZ4K=y                   # LZ4K 加密 API
          CONFIG_CRYPTO_LZ4KD=y                  # LZ4KD 加密 API
          
          # -----------------------------------------------------------------
          # 9. File System Features and Hardware Support
          # -----------------------------------------------------------------
          CONFIG_TMPFS_POSIX_ACL=y               # TMPFS: POSIX ACL
          CONFIG_TMPFS_XATTR=y                   # TMPFS: 扩展属性
          CONFIG_BT_QCA=y                        # 高通蓝牙驱动
          CONFIG_NVME_MULTIPATH=y                # NVMe 多路径支持
          CONFIG_NVME_TARGET=y                   # NVMe 目标支持
          
EOF
          
          # 移除构建流程中的 check_defconfig 校验
          sed -i 's/check_defconfig//' ./common/build.config.gki
          
          # 提交更改（可选）
          cd common
          git add -A
          git commit -a -m "BUILD Kernel with Polly, BBR, KSU, compression configs"

      - name: Add make name
        run: |
         cd kernel_workspace/kernel_platform
         sed -i '208s|echo ".*"|echo "${KERNELVERSION}${scm_version}${config_localversion}"|' common/scripts/setlocalversion
         sed -i "s/\${scm_version}/${{ github.event.inputs.KERNEL_NAME }}/g" ./common/scripts/setlocalversion
         
      - name: 设置 CONFIG_LOCALVERSION
        run: |
         cd kernel_workspace/kernel_platform
         echo 'CONFIG_LOCALVERSION="${{ github.event.inputs.local_version }}"' >> ./common/arch/arm64/configs/gki_defconfig

      - name: 删除 CONFIG_LOCALVERSION 中的 -4k 后缀（如果勾选）
        if: ${{ fromJSON(github.event.inputs.remove_default_4k) }}
        run: |
         cd kernel_workspace/kernel_platform
         sed -i 's/-4k//' ./common/arch/arm64/configs/gki_defconfig

      - name: 拉取afdo
        run: |
          cd kernel_workspace/kernel_platform/
          git clone https://github.com/PxxPei/afdo.git

      - name: Set build timestamp
        run: |         
         export SOURCE_DATE_EPOCH=$(date -d "Tue Jul  1 19:48:18 UTC 2025" +%s)
         echo "SOURCE_DATE_EPOCH=$SOURCE_DATE_EPOCH" >> $GITHUB_ENV
   
      - name: Build Kernel
        run: |
         # 🧩 下载并配置 Clang 19.0.0git（保留系统 Clang）
         # ===============================

         # 进入 clang 预编译目录
         cd ${{ github.workspace }}/kernel_workspace/kernel_platform/prebuilts/clang/host/linux-x86
         
         echo "⬇️ 下载 Clang 19.0.0git..."
         wget -q https://github.com/ZyCromerZ/Clang/releases/download/19.0.0git-20240723-release/Clang-19.0.0git-20240723.tar.gz

         echo "📦 解压中..."
         # 解压到指定目录 clang-19.0.0git
         mkdir -p clang-19.0.0git
         tar -xf Clang-19.0.0git-20240723.tar.gz -C clang-19.0.0git

         echo "📂 解压后目录结构："
         ls -l clang-19.0.0git

         # 将下载的 Clang 添加到 PATH（优先使用下载的版本）
         export PATH="$(pwd)/clang-19.0.0git/bin:$PATH"

         # 验证 Clang 版本是否正确加载
         echo "🔍 当前 Clang 版本："
         clang --version || { echo "❌ 未找到 clang，请检查 PATH！"; exit 1; }

         # 验证 clang 版本是否为 19 开头
         CLANG_VERSION=$(clang --version | head -n 1 | grep -o "19\.")
         if [ -z "$CLANG_VERSION" ]; then
            echo "⚠️ 警告：当前 clang 版本不是 19.x，可能 PATH 配置错误！"
         fi

         echo "✅ Clang 环境配置完成"
         # 回到源码目录
         cd ${{ github.workspace }}/kernel_workspace/kernel_platform/common || { echo "❌ 源码目录不存在"; exit 1; }
         # 同时加入 ccache（如果存在）
         export PATH="/usr/lib/ccache:$PATH"

         # 设置固定内核构建时间
         export KERNEL_TIME="Tue Jul  1 19:48:18 UTC 2025"
         export KBUILD_BUILD_TIMESTAMP="$KERNEL_TIME"
         export SOURCE_DATE_EPOCH=$(date -d "$KERNEL_TIME" +%s)
         # 设置 AFDO 配置文件路径
         export AFDO_PROFILE="${{ github.workspace }}/kernel_workspace/kernel_platform/afdo/kernel.afdo"
         if [ ! -f "$AFDO_PROFILE" ]; then
           echo "Error: AFDO profile not found at $AFDO_PROFILE"
         exit 1
         fi

         # 设置一些通用参数（便于后续维护）
         export KCFLAGS="-Wno-error -fauto-profile=$AFDO_PROFILE"
         export MAKE_ARGS="LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang \
         RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc \
         PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole \
         LD=ld.lld HOSTLD=ld.lld O=out SOURCE_DATE_EPOCH=${SOURCE_DATE_EPOCH}"

         # 进入源码目录
         cd ${{ github.workspace }}/kernel_workspace/kernel_platform/common || { echo "❌ 源码目录不存在"; exit 1; }
         rm -rf out

         # 执行 defconfig
         export KBUILD_CFLAGS="-fpolly -flto=thin"
         make -j$(nproc) $MAKE_ARGS gki_defconfig

         # 编译全部目标
         make -j$(nproc) $MAKE_ARGS all

         # 创建输出目录并复制 Image
         mkdir -p ../dist
         cp out/arch/arm64/boot/Image ../dist/
         
      - name: 验证内核构建时间
        run: |
         strings kernel_workspace/kernel_platform/common/out/vmlinux | grep "UTC"

      - name: Prepare AnyKernel3 package
        run: |
          cd kernel_workspace/kernel_platform/dist
          git clone https://github.com/3620603660/AnyKernel3.git AnyKernel3 --depth=1
          rm -rf AnyKernel3/.git
          rm -f AnyKernel3/push.sh
          cp Image AnyKernel3/
          
      - name: Upload AnyKernel3 artifact
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3_oneplus_ace5pro_${{ env.KSUVER }}_(kowsu)noroot
          path: kernel_workspace/kernel_platform/dist/AnyKernel3/*

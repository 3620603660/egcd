name: ace5pro_kernel_测试6.6合并新10ⅹpro

on:
  workflow_dispatch:
    inputs:
      CPU_ARCH:
        description: "目标CPU架构分支，例如 sm8750 (默认: sm8750)"
        required: true
        default: 'sm8750'
      DEVICE_CONFIG:
        description: "设备配置文件名称，例如 oneplus_ace5_pro (默认: oneplus_ace5_pro)"
        required: true
        default: 'oneplus_ace5_pro_pro'
      ANDROID_TARGET_VERSION:
        description: "目标Android版本，例如 android15 (默认: android15)"
        required: true
        default: 'android15'
      KERNEL_BASE_VERSION:
        description: "内核基础版本，例如 6.6 (默认: 6.6)"
        required: true
        default: '6.6'
      CUSTOM_KERNEL_NAME_TAG:
        description: "自定义内核名称标识，将附加到内核版本号后 (例如: -android15-8-g013ec21bba94-abogki383916444)"
        required: true
        default: '-android15-8-g29d86c5fc9dd-abogki428889875-4k'
      LOCAL_VERSION_SUFFIX:
        description: '内核版本后缀（如-v8，默认：-4k）'
        required: false
        default: ''
        type: string
      REMOVE_DEFAULT_4K_SUFFIX:
        description: '是否删除默认的 -4k 后缀？（勾选则删除）'
        required: false
        default: true
        type: boolean
      BUILD_TIMESTAMP:
        description: '内核构建时间 (UTC格式，例如 "Tue Dec 17 23:36:49 UTC 2024")'
        required: false
        default: 'Mon May 12 09:09:59 UTC 2025'
        type: string

env:
  WORKSPACE_DIR: kernel_workspace
  KERNEL_PLATFORM_DIR: kernel_workspace/kernel_platform
  CCACHE_DIR: /home/runner/.ccache
  CCACHE_MAXSIZE: 8G
  CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
  CCACHE_NOHASHDIR: "true"
  CCACHE_HARDLINK: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    
    env:
      TOKEN: ${{ secrets.TOKEN }}
    
    steps:
      - name: 1.1 最大化构建空间
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          
      - name: 1.2 配置Git用户信息
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: 1.3 检出工作流仓库代码  
        uses: actions/checkout@v4  

      - name: 1.4 安装构建核心依赖  
        run: |  
          sudo apt-get update  
          sudo apt-get install -y --no-install-recommends \
            python3 git curl ccache libelf-dev build-essential \
            bc bison flex libssl-dev libncurses5-dev

      - name: 2.1 恢复 ccache 缓存
        uses: actions/cache@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ runner.os }}-${{ github.repository }}-${{ github.ref_name }}-v3_pro
          restore-keys: |
            ${{ runner.os }}-${{ github.repository }}-${{ github.ref_name }}-ccache-
            ${{ runner.os }}-${{ github.repository }}-ccache-
            ${{ runner.os }}-ccache-
    
      - name: 2.2 配置 ccache
        run: |  
          echo "CCACHE_BASEDIR=${{ github.workspace }}" >> $GITHUB_ENV  
          echo "CCACHE_LOGFILE=${{ github.workspace }}/ccache.log" >> $GITHUB_ENV  
          echo "/usr/lib/ccache" >> $GITHUB_PATH  
         
      - name: 2.3 显示 ccache 统计信息
        run: |
          ccache -s || true
         
      - name: 3.1 安装 repo 工具
        run: |
          curl -sSL https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
          chmod a+x ~/repo
          sudo mv ~/repo /usr/local/bin/repo

      - name: 3.2 初始化并同步内核源码
        run: |
          mkdir -p ${{ env.WORKSPACE_DIR }} && cd ${{ env.WORKSPACE_DIR }}
          repo init -u https://github.com/3620603660/kernel_manifest.git \
            -b refs/heads/oneplus/${{ github.event.inputs.CPU_ARCH }} \
            -m ${{ github.event.inputs.DEVICE_CONFIG }}.xml --depth=1
          repo sync -c -j$(nproc) --no-tags --no-clone-bundle
          
          # 清理不需要的文件
          rm -f kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports to remove."
          rm -f kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports to remove."
         
      - name: Set up SukiSU
        run: |
         cd kernel_workspace/kernel_platform
         curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main
         cd ./KernelSU
         KSU_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 10606)
         echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
         export KSU_VERSION=$KSU_VERSION
         sed -i "s/DKSU_VERSION=12800/DKSU_VERSION=${KSU_VERSION}/" kernel/Makefile
         KSU_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 10700)
         echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
         export KSU_VERSION=$KSU_VERSION

      - name: Set up SUSFS & apply patches
        run: |
         set -e
         
         cd kernel_workspace
         git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-${{ github.event.inputs.ANDROID_TARGET_VERSION }}-${{ github.event.inputs.KERNEL_BASE_VERSION }}
         git clone --depth=1 https://github.com/SukiSU-Ultra/SukiSU_patch.git
         git clone https://$TOKEN@github.com/3620603660/boot.git
         cd kernel_platform
         cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_TARGET_VERSION }}-${{ github.event.inputs.KERNEL_BASE_VERSION }}.patch ./common/ || true
         mkdir -p ./common/fs ./common/include/linux ./common/lib ./common/crypto
         cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/ || true
         cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/ || true
         cp -r ../SukiSU_patch/other/zram/lz4k/include/linux/* ./common/include/linux || true
         cp -r ../SukiSU_patch/other/zram/lz4k/lib/* ./common/lib || true
         cp -r ../SukiSU_patch/other/zram/lz4k/crypto/* ./common/crypto || true
         cp -r ../SukiSU_patch/other/zram/lz4k_oplus ./common/lib/ || true
         cp ../boot/1.patch ./common/
         cd ./common
         PATCH_FILE=50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_TARGET_VERSION }}-${{ github.event.inputs.KERNEL_BASE_VERSION }}.patch
         if [[ -f "$PATCH_FILE" ]]; then
         # sed -i 's/-32,12 +32,38/-32,11 +32,37/g' "$PATCH_FILE"
         # sed -i '/#include <trace\/hooks\/fs.h>/d' "$PATCH_FILE"
         patch -p1 < "$PATCH_FILE" || true
         fi
         cp ../../SukiSU_patch/hooks/syscall_hooks.patch ./
         patch -p1 -F 3 < syscall_hooks.patch || true
         patch -s -p1 -F 3 < 1.patch
         echo "✅ SUSFS 和 syscall_hooks patch 应用完成"

      - name: Apply Hide Stuff Patches
        run: |
          cd kernel_workspace/kernel_platform/common
          cp ../../SukiSU_patch/69_hide_stuff.patch ./
          echo "正在打隐藏应用补丁"
          patch -p1 -F 3 < 69_hide_stuff.patch
          echo "隐藏应用_patch完成"
         
      - name: 设置内核优化配置                                                      # 定义任务名称
        run: |                                                                     # 开始执行多行命令
          cd ${{ env.KERNEL_PLATFORM_DIR }}                                       # 切换到内核平台目录
          
          # 定义配置文件路径                                                      # 注释：设置配置文件路径变量
          CONFIG_FILE="./common/arch/arm64/configs/gki_defconfig"                 # 指定内核配置文件路径
          
          # 检查配置文件是否存在                                                  # 注释：验证配置文件存在性
          if [ ! -f "$CONFIG_FILE" ]; then                                        # 条件判断文件是否存在
            echo "Error: 配置文件 $CONFIG_FILE 不存在"                            # 输出错误信息
            exit 1                                                                # 退出并返回错误代码
          fi                                                                      # 结束条件判断
          
          # 添加优化配置                                                          # 注释：开始追加配置内容
          cat >> "$CONFIG_FILE" << 'EOF'           # 使用here文档追加内容到配置文件
         
          # KernelSU 配置

          CONFIG_KSU=y
          
          CONFIG_KSU_SUSFS_SUS_SU=n
          CONFIG_KSU_MANUAL_HOOK=y
          
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          
          # CPU和电源管理                                                        # 注释：CPU相关配置节
          CONFIG_CPU_FREQ=y                                                       # 启用CPU频率调节
          CONFIG_CPU_FREQ_STAT=y                                                  # 启用CPU频率统计
          CONFIG_CPU_IDLE=y                                                       # 启用CPU空闲状态管理
          CONFIG_PM_WAKELOCKS=y                                                   # 启用电源管理唤醒锁
          CONFIG_PM_WAKELOCKS_GC=y                                                # 启用唤醒锁垃圾回收
          CONFIG_SCHED_CORE=y                                                     # 启用核心调度功能
          CONFIG_SCHED_MC=y                                                       # 启用多核调度
          CONFIG_UCLAMP_TASK=y                                                    # 启用任务利用率钳制
          CONFIG_SCHED_HRTICK=y                                                   # 启用高精度调度时钟
          CONFIG_CPU_IDLE_GOV_TEO=y                                               # 启用TEO空闲状态调控器
          
          # 内存管理                                                              # 注释：内存管理配置节
          CONFIG_LRU_GEN=y                                                        # 启用新一代LRU算法
          CONFIG_TRANSPARENT_HUGEPAGE=y                                           # 启用透明大页支持
          CONFIG_TRANSPARENT_HUGEPAGE_ALWAYS=n                                    # 禁用始终使用大页
          CONFIG_TRANSPARENT_HUGEPAGE_MADVISE=y                                   # 启用madvise大页建议
          
          # 网络性能优化 (BBR3)                                                   # 注释：网络优化配置节
          CONFIG_BQL=y                                                            # 启用字节队列限制
          CONFIG_INET=y                                                           # 启用IPv4协议支持
          CONFIG_IPV6=y                                                           # 启用IPv6协议支持
          CONFIG_NET=y                                                            # 启用网络支持
          CONFIG_NET_SCHED=y                                                      # 启用网络流量调度
          CONFIG_NET_SCH_CAKE=y                                                   # 启用CAKE队列管理
          CONFIG_NET_SCH_FQ=y                                                     # 启用公平队列
          CONFIG_NET_SCH_FQ_CODEL=y                                               # 启用FQ_CODEL队列管理
          CONFIG_DEFAULT_NET_SCH="cake"                                           # 设置默认网络调度器为CAKE
          CONFIG_DEFAULT_TCP_CONG="bbr"                                           # 设置默认TCP拥塞控制为BBR
          CONFIG_TCP_CONG_BBR=y                                                   # 启用BBR拥塞控制算法
          CONFIG_TCP_CONG_BIC=n                                                   # 禁用BIC拥塞控制算法
          CONFIG_TCP_CONG_CUBIC=n                                                 # 禁用CUBIC拥塞控制算法
          CONFIG_TCP_CONG_HTCP=n                                                  # 禁用HTCP拥塞控制算法
          CONFIG_TCP_CONG_WESTWOOD=n                                              # 禁用Westwood拥塞控制算法
          CONFIG_TCP_FASTOPEN=y                                                   # 启用TCP快速打开
          CONFIG_TCP_SACK=y                                                       # 启用TCP选择性确认
          CONFIG_TCP_TIMESTAMPS=y                                                 # 启用TCP时间戳
          
          # ADIOS和O2优化配置                                                     # 注释：IO优化配置节
          CONFIG_IO_URING=y                                                       # 启用io_uring异步IO接口
          CONFIG_BLK_MQ=y                                                         # 启用多队列块设备
          CONFIG_BLK_MQ_PCI=y                                                     # 启用PCI多队列支持
          CONFIG_O2_OPTIMIZATION=y                                                # 启用O2级别优化
          CONFIG_MQ_IOSCHED_ADIOS=y                                               # 启用ADIOS IO调度器
          
          # 文件系统压缩支持                                                      # 注释：压缩算法配置节
          CONFIG_CRYPTO_LZ4=y                                                     # 启用LZ4压缩算法
          CONFIG_CRYPTO_LZ4HC=y                                                   # 启用LZ4HC高压缩比算法
          CONFIG_CRYPTO_LZ4KD=y                                                   # 启用LZ4KD压缩算法
          CONFIG_CRYPTO_ZSTD=y                                                    # 启用ZSTD压缩算法
          CONFIG_F2FS_FS_LZ4=y                                                    # 启用F2FS文件系统LZ4支持
          CONFIG_F2FS_FS_LZ4HC=y                                                  # 启用F2FS文件系统LZ4HC支持
          CONFIG_F2FS_FS_ZSTD=y                                                   # 启用F2FS文件系统ZSTD支持
          
          # 编译优化                                                              # 注释：编译优化配置节
          CONFIG_LTO_CLANG=y                                                      # 启用Clang LTO链接时优化
          CONFIG_LTO_CLANG_THIN=y                                                 # 启用Clang ThinLTO优化
          CONFIG_LTO_POLLY=y                                                      # 启用Polly循环优化
          
          EOF                                                                     # 结束here文档
          
          # 禁用defconfig检查                                                     # 注释：修改构建配置
          sed -i 's/check_defconfig//' ./common/build.config.gki                  # 移除defconfig检查步骤
          
          # 提交更改                                                              # 注释：提交配置更改
          git add -A && git commit -a -m "BUILD Kernel with optimizations" || true # 添加并提交所有更改，忽略错误
          
          # 日志记录                                                              # 注释：输出完成信息
          echo "✅ 内核优化配置已完成"                                            # 显示成功完成消息

      - name: 7.1 修改内核版本显示名称
        run: |
          cd ${{ env.KERNEL_PLATFORM_DIR }}
          sed -i '186s|echo ".*"|echo "${KERNELVERSION}${scm_version}${config_localversion}"|' common/scripts/setlocalversion
          sed -i "s/\${scm_version}/${{ github.event.inputs.CUSTOM_KERNEL_NAME_TAG }}/g" ./common/scripts/setlocalversion
         
      - name: 7.2 设置 CONFIG_LOCALVERSION
        run: |
          cd ${{ env.KERNEL_PLATFORM_DIR }}
          echo 'CONFIG_LOCALVERSION="${{ github.event.inputs.LOCAL_VERSION_SUFFIX }}"' >> ./common/arch/arm64/configs/gki_defconfig

      - name: 7.3 删除 CONFIG_LOCALVERSION 中的 -4k 后缀（如果勾选）
        if: ${{ fromJSON(github.event.inputs.REMOVE_DEFAULT_4K_SUFFIX) }}
        run: |
          cd ${{ env.KERNEL_PLATFORM_DIR }}
          sed -i 's/-4k//' ./common/arch/arm64/configs/gki_defconfig

      - name: 8.1 设置构建时间戳
        run: |         
          export SOURCE_DATE_EPOCH=$(date -d "${{ github.event.inputs.BUILD_TIMESTAMP }}" +%s)
          echo "SOURCE_DATE_EPOCH=$SOURCE_DATE_EPOCH" >> $GITHUB_ENV
      
      - name: Add sched_ext
        run: |
         cd kernel_workspace/kernel_platform/
         git clone https://github.com/3620603660/sched_ext.git
         cp -r ./sched_ext/* ./common/kernel/sched
         rm -rf ./sched_ext/.git

      - name: 8.2 拉取afdo
        run: |
          cd ${{ env.WORKSPACE_DIR }}
          git clone https://github.com/PxxPei/afdo.git

      - name: 9.1 安装和设置 Clang 19.0.0 编译器环境
        run: |
          mkdir -p clang19
          cd clang19
          wget -q https://github.com/ZyCromerZ/Clang/releases/download/19.0.0git-20240723-release/Clang-19.0.0git-20240723.tar.gz
          tar -zxf Clang-19.0.0git-20240723.tar.gz
          echo "CLANG_PATH=${{ github.workspace }}/clang19/bin" >> $GITHUB_ENV

      - name: 10.1 编译内核
        run: |
          # 设置编译环境
          export PATH="${{ env.CLANG_PATH }}:${PATH}"
          export PATH="/usr/lib/ccache:${PATH}"
          
          # 设置时间戳
          export KERNEL_TIME="${{ github.event.inputs.BUILD_TIMESTAMP }}"
          export KBUILD_BUILD_TIMESTAMP="$KERNEL_TIME"
          export SOURCE_DATE_EPOCH=${{ env.SOURCE_DATE_EPOCH }}

          # 设置AFDO配置文件
          export AFDO_PROFILE="${{ github.workspace }}/${{ env.WORKSPACE_DIR }}/afdo/kernel.afdo"
          if [ ! -f "$AFDO_PROFILE" ]; then
             echo "Error: AFDO profile not found at $AFDO_PROFILE"
              exit 1
          fi
          
          export KCFLAGS="-Wno-error -mllvm -thinlto-assume-merged"
          echo "Using KCFLAGS: ${KCFLAGS}"
          echo "Using KCFLAGS: -Wno-error -fauto-profile=$AFDO_PROFILE"
          
          # 设置编译参数
          export CC="ccache clang"
          export KCFLAGS="-Wno-error -fauto-profile=$AFDO_PROFILE"
          export MAKE_ARGS="LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang \
          RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc \
          PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole \
          LD=ld.lld HOSTLD=ld.lld O=out SOURCE_DATE_EPOCH=${SOURCE_DATE_EPOCH}"
          
          # 进入编译目录
          cd ${{ env.KERNEL_PLATFORM_DIR }}/common
          rm -rf out
          
          # 开始编译
          echo "开始编译内核..."
          # 确保 KBUILD_CFLAGS 包含 Polly 和 LTO 设置
          export KBUILD_CFLAGS="-O2 -fpolly -flto=thin"
          make $MAKE_ARGS gki_defconfig
          make $MAKE_ARGS -j$(nproc)

          echo "✅ 内核编译完成"

      - name: 11.1 显示编译结果
        run: |
          cd ${{ env.KERNEL_PLATFORM_DIR }}/common
          if [ -f "out/arch/arm64/boot/Image" ]; then
            echo "✅ 内核镜像编译成功"
            ls -lh out/arch/arm64/boot/Image*
          else
            echo "❌ 内核镜像编译失败"
            exit 1
          fi

          mkdir -p ../dist
          cp out/arch/arm64/boot/Image ../dist/
          echo "✅ 内核编译成功！产物已复制到 dist 目录。"
          
      - name: 10.2 验证内核构建时间
        run: |
         strings kernel_workspace/kernel_platform/common/out/vmlinux | grep "UTC"

      - name: 11.1 准备 AnyKernel3 刷机包
        run: |
          cd kernel_workspace/kernel_platform/dist
          git clone https://github.com/3620603660/AnyKernel3.git AnyKernel3 --depth=1
          rm -rf AnyKernel3/.git
          rm -f AnyKernel3/push.sh
          cp Image AnyKernel3/

      - name: 11.2 上传编译产物到 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3_${{ github.event.inputs.DEVICE_CONFIG }}_${{ env.KSUVER }}
          path: kernel_workspace/kernel_platform/dist/AnyKernel3/*
          
      - name: 11.2 显示最终 ccache 统计信息
        run: |
          ccache -s





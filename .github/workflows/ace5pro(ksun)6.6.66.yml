name: ace5pro(ksun)6.6.66
on:
  workflow_dispatch:
    inputs:
      CPU:
        description: "分支"
        required: true
        default: 'sm8750'
      FEIL:
        description: "配置文件"
        required: true
        default: 'oneplus_ace5_pro'
      ANDROID_VERSION:
        description: "内核安卓版本"
        required: true
        default: 'android15'
      KERNEL_VERSION:
        description: "内核版本"
        required: true
        default: '6.6'
      KERNEL_NAME:
        description: "修改内核名称"
        required: true
        default: '-android15-8-g29d86c5fc9dd-abogki428889875'
      local_version:
        description: '输入内核后缀名（如-v8）'
        required: false
        default: '-4k'
        type: string
      remove_default_4k:
        description: '是否删除默认的 -4k 后缀？（勾选则删除）'
        required: false
        default: false
        type: boolean
      kernel_time:
        description: '内核构建时间（UTC 时间字符串，例如 "Mon May 12 09:09:59 UTC 2025"）'
        required: false
        default: 'Tue Jul  1 19:48:18 UTC 2025'
        type: string
      KernelSU_Nex_tag:
        description: "自定义版本来源后缀（如:见清）"
        required: false
        default: "-by-Ciallo～(∠・ω<)⌒★"
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      TOKEN: ${{ secrets.TOKEN }}
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          
      - name: Configure Git
        run: |
         git config --global user.name "build"
         git config --global user.email "3620603668@qq.com"

      # 1. 拉取仓库  
      - name: Checkout  
        uses: actions/checkout@v4  

      # 2. 安装构建依赖（包括 ccache）  
      - name: Install Dependencies  
        run: |  
         sudo apt-get update  
         sudo apt-get install -y python3 git curl ccache libelf-dev
         sudo apt-get install -y dos2unix  

      # 3. 恢复 ccache 缓存  
      - name: Restore ccache
        uses: actions/cache@v3
        with:
         path: /home/runner/.ccache
         key: ${{ runner.os }}-${{ github.repository }}-v2_ksun
         restore-keys: |
          ${{ runner.os }}-${{ github.repository }}-
          ${{ runner.os }}-
          ccache-

      # 4. 设置 ccache 环境变量  
      - name: Setup ccache environment  
        run: |  
         echo "CCACHE_DIR=/home/runner/.ccache" >> $GITHUB_ENV  
         echo "CCACHE_MAXSIZE=8G" >> $GITHUB_ENV  
         echo "CCACHE_COMPILERCHECK=%compiler% -dumpmachine; %compiler% -dumpversion" >> $GITHUB_ENV  
         echo "CCACHE_NOHASHDIR=true" >> $GITHUB_ENV  
         echo "CCACHE_HARDLINK=true" >> $GITHUB_ENV  
         echo "CCACHE_BASEDIR=${{ github.workspace }}" >> $GITHUB_ENV  
         echo "CCACHE_LOGFILE=${{ github.workspace }}/ccache.log" >> $GITHUB_ENV  
         echo "/usr/lib/ccache" >> $GITHUB_PATH  
         
      - name: Show ccache stats
        run: |
         ccache -s || true

      - name: Install repo tool
        run: |
         curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
         chmod a+x ~/repo
         sudo mv ~/repo /usr/local/bin/repo

      - name: Initialize repo and sync
        run: |
         mkdir kernel_workspace && cd kernel_workspace
         repo init -u https://github.com/OnePlusOSS/kernel_manifest.git -b refs/heads/oneplus/${{ github.event.inputs.CPU }} -m ${{ github.event.inputs.FEIL }}.xml --depth=1
         repo --trace sync -c -j$(nproc --all) --no-tags
         rm kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
         rm kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"

      - name: Export suffix
        run: |
          echo "TAG_SUFFIX=${{ github.event.inputs.KernelSU_Nex_tag }}" >> $GITHUB_ENV
          
      - name: Setup KernelSU-Next
        run: |
          cd kernel_workspace/kernel_platform
          curl -LSs "https://raw.githubusercontent.com/pershoot/KernelSU-Next/next-susfs/kernel/setup.sh" | bash -s next-susfs
          cd KernelSU-Next

          # 获取 next 分支 commit 数量并加上 10200
          KSU_VERSION=$(expr $(curl -sI "https://api.github.com/repos/KernelSU-Next/KernelSU-Next/commits?sha=next&per_page=1" \
            | grep -i "link:" \
            | sed -n 's/.*page=\([0-9]*\)>; rel="last".*/\1/p') "+" 10200)

          {
            echo "KSUVER<<EOF"
            echo "${KSU_VERSION}"
            echo "EOF"
          } >> $GITHUB_ENV

          sed -i "s/DKSU_VERSION=11998/DKSU_VERSION=${KSU_VERSION}/" kernel/Makefile

          # 获取最近的 tag (例: v1.1.1)
          TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")

          # 拼接 tag+后缀（后缀来自 Export suffix 步骤注入）
          CUSTOM_TAG="${TAG}${TAG_SUFFIX}"

          {
            echo "KSUTAG<<EOF"
            echo "${CUSTOM_TAG}"
            echo "EOF"
          } >> $GITHUB_ENV

          # 替换 Makefile 里的默认 DKSU_VERSION_TAG
          sed -i "s|^\s*KSU_VERSION_TAG :=.*|KSU_VERSION_TAG := ${CUSTOM_TAG}|" kernel/Makefile
          
          # 打印确认
          echo "👉 KernelSU-Next Tag = ${CUSTOM_TAG}"
          
      - name: Set up SUSFS & apply patches
        run: |
         set -e
         cd kernel_workspace
         git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-${{ inputs.ANDROID_VERSION }}-${{ inputs.KERNEL_VERSION }}
         git clone --depth=1 https://github.com/SukiSU-Ultra/SukiSU_patch.git
         git clone https://$TOKEN@github.com/3620603660/boot/1.git
         git clone https://github.com/WildPlusKernel/kernel_patches.git
         git clone https://github.com/egcd521/GKI_KernelSU_SUSFS.git zzh_patch
         cd kernel_platform
         echo "正在拉取susfs补丁"
         cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}.patch ./common/
         cp ../kernel_patches/next/scope_min_manual_hooks_v1.4.patch ./common/
         cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/
         cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/
         cp ../zzh_patch/next-patch/* ./KernelSU-Next/
         cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/
         cp -r ../zzh_patch/zram/lz4/* ./common/lib/lz4/
         cp -r ../zzh_patch/zram/include/linux/* ./common/include/linux/
         cp -r ../zzh_patch/zram/${{ inputs.kernel_version }}/lz4_1.10.0.patch ./common/
         cd ./common
         for file in \
         "lib/lz4/lz4_compress.c" \
         "lib/lz4/lz4_decompress.c" \
         "lib/lz4/lz4defs.h" \
         "lib/lz4/lz4hc_compress.c"
         do
         if [ -f "$file" ]; then
         echo "🗑️ 正在删除：$file"
         rm -f "$file"
         else
         echo "⚠️ 文件不存在：$file"
         fi
         done
         cd ../
         cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/
         cp -r ../SukiSU_patch/other/zram/lz4k/include/linux/* ./common/include/linux || true
         cp -r ../SukiSU_patch/other/zram/lz4k/lib/* ./common/lib || true
         cp -r ../SukiSU_patch/other/zram/lz4k/crypto/* ./common/crypto || true
         cp -r ../SukiSU_patch/other/zram/lz4k_oplus ./common/lib/ || true
         cp ../boot/1.patch ./common/
         cd ./common/
         # if [[ "${{ github.event.inputs.FEIL }}" == "oneplus_13" || "${{ github.event.inputs.FEIL }}" == "oneplus_ace5_pro" ]]; then
         # sed -i 's/-32,12 +32,38/-32,11 +32,37/g' 50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}.patch
         # sed -i '/#include <trace\/hooks\/fs.h>/d' 50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}.patch
         # fi
          patch -p1 < 50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}.patch || true
          # Replace next_hooks.patch with syscall_hooks.patch
          cp ../../kernel_patches/69_hide_stuff.patch ./
          patch -p1 -F 3 < 69_hide_stuff.patch
          patch -p1 --fuzz=3 < scope_min_manual_hooks_v1.4.patch
          patch -s -p1 -F 3 < 1.patch || true
          patch -p1 -F 3 --fuzz=5 < lz4_1.10.0.patch || true
          echo "✅ SUSFS 和 scope_min_manual_hooks_v1.4 patch 应用完成"         
          
      - name: 应用lz4 1.10.0 & zstd 1.5.7补丁
        run: |
         echo "正在添加lz4 1.10.0 & zstd 1.5.7补丁…"
         # 进入内核工作区
         cd kernel_workspace
         # 克隆补丁仓库
         git clone https://github.com/PxxPei/oppo_oplus_realme_sm8750.git
         # --- 新增步骤：确保目标目录存在 ---
         mkdir -p ./common/lib
         # 复制补丁文件到正确的位置
         cp ./oppo_oplus_realme_sm8750/zram_patch/001-lz4.patch ./common
         cp ./oppo_oplus_realme_sm8750/zram_patch/lz4armv8.S ./common/lib
         cp ./oppo_oplus_realme_sm8750/zram_patch/002-zstd.patch ./common
         # 进入 common 目录以应用补丁
         cd ./common
         # 应用补丁
         git apply 001-lz4.patch || true
         patch -p1 < 002-zstd.patch || true
         
      - name: Apply lz4kd Patches
        run: |
         set -e
         cd kernel_workspace/kernel_platform/common
         PATCH_FILE=../../SukiSU_patch/other/zram/zram_patch/${{ inputs.KERNEL_VERSION }}/lz4kd.patch
         if [[ -f "$PATCH_FILE" ]]; then
         cp "$PATCH_FILE" ./lz4kd.patch
         patch -p1 -F 3 < lz4kd.patch || true
         else
         echo "❗ lz4kd patch not found for kernel version ${{ inputs.KERNEL_VERSION }}"
         fi

      - name: 设置内核优化配置
        run: |
          cd kernel_workspace/kernel_platform
          # 向 gki_defconfig 中追加配置
          cat <<EOF >> ./common/arch/arm64/configs/gki_defconfig
          # KSU 与 SUSFS 配置
          CONFIG_KSU=y
          CONFIG_KSU_MANUAL_HOOK=n
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_SU=n
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n
          
          #### CPU和电源管理

          CONFIG_CPUFREQ_BOOST=y          # 启用CPU频率提升
          CONFIG_CPU_FREQ=y          # 启用CPU频率调节
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y          # 启用基于调度器利用率信息的CPU频率调速器
          CONFIG_CPU_FREQ_STAT=y          # 收集CPU频率转换的统计信息
          CONFIG_CPU_HOTPLUG=y          # 启用CPU热插拔
          CONFIG_CPU_IDLE=y          # 启用CPU空闲状态管理
          CONFIG_CPU_IDLE_GOV_TEO=y          # 启用TEO（Timer Events Oriented）空闲调速器
          CONFIG_CPU_IDLE_STATE_RESIDENCY=y          # 跟踪CPU在每个空闲状态的驻留时间
          CONFIG_CPU_IDLE_TIME_IN_ACCOUNTING=y          # 将CPU空闲时间计入账户
          CONFIG_DEVFREQ_BOOST=y          # 启用设备频率提升
          CONFIG_PM_DOMAIN_CPU_GOV=y          # 启用CPU电源域调速器
          CONFIG_PM_WAKELOCKS=y          # 启用唤醒锁，防止系统进入深度睡眠
          CONFIG_PM_WAKELOCKS_GC=y          # 启用唤醒锁的垃圾回收
          CONFIG_QCOM_CPU_CLUSTER_POWER_CONTROL=y          # 启用高通CPU集群功耗控制
          CONFIG_SCHED_CLASS_EXT=y          # 启用扩展调度类
          CONFIG_SCHED_CORE=y          # 启用核心调度，允许更精细的CPU资源控制
          CONFIG_SCHED_EAS=y          # 启用Energy Aware Scheduling (EAS)
          CONFIG_SCHED_HRTICK=y          # 启用高精度调度时钟
          CONFIG_SCHED_MC=y          # 启用多核调度
          CONFIG_SCHED_THERMAL_PRESSURE=y          # 允许调度器感知热压力
          CONFIG_SCHED_TUNE=y          # 启用调度器调优
          CONFIG_SCHED_UTIL_CLAMP=y          # 启用调度利用率限制
          CONFIG_SCHED_WALT=y          # 启用Window-Assisted Load Tracking (WALT)调度算法
          CONFIG_THERMAL_GOV_FAIR_SHARE=y          # 启用公平分享散热调速器
          CONFIG_THERMAL_GOV_POWER_ALLOCATOR=y          # 启用电源分配散热调速器
          CONFIG_UCLAMP_TASK=y          # 允许任务级别的利用率限制

          #### 内存管理

          CONFIG_ANDROID_LOW_MEMORY_KILLER=y          # 启用Android的低内存杀手
          CONFIG_BLK_DEV_ZRAM_DISCARD=y          # 允许zRAM设备处理discard请求
          CONFIG_LRU_GEN=y          # 启用多代LRU（最近最少使用）算法
          CONFIG_TRANSPARENT_HUGEPAGE=y          # 启用透明大页，提高内存管理效率
          CONFIG_TRANSPARENT_HUGEPAGE_ALWAYS=n          # 不总是强制使用透明大页
          CONFIG_TRANSPARENT_HUGEPAGE_MADVISE=y          # 允许通过madvise()系统调用选择性使用透明大页
          CONFIG_ZRAM=y          # 启用zRAM，创建基于RAM的压缩块设备
          CONFIG_ZRAM_LZ4=y          # 为zRAM启用LZ4压缩算法
          CONFIG_ZRAM_MULTI_COMP=y          # 允许zRAM使用多种压缩算法
          CONFIG_ZRAM_ZSTD=y          # 为zRAM启用Zstandard压缩算法
          CONFIG_ZSMALLOC=y          # 启用zsmalloc，一种用于压缩页面的内存分配器

          #### 编译与性能优化

          CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y          # 使用-O2编译器标志进行性能优化
          CONFIG_LOCALVERSION_AUTO=n          # 禁止自动在内核版本后附加git commit ID
          CONFIG_LTO_CLANG=y
          CONFIG_LTO_CLANG_THIN=y
          CONFIG_LTO_POLLY=y

          #### 网络

          CONFIG_BPF_STREAM_PARSER=y          # 启用BPF流解析器，用于TCP流处理
          CONFIG_BQL=y          # 启用Byte Queue Limits，用于网络设备
          CONFIG_DEFAULT_NET_SCH="cake"
          CONFIG_DEFAULT_TCP_CONG="bbr"
          CONFIG_INET=y          # 启用TCP/IP协议栈
          CONFIG_IP6_NF_NAT=y          # 启用IPv6网络地址转换（NAT）
          CONFIG_IP6_NF_TARGET_MASQUERADE=y          # 启用IPv6的MASQUERADE NAT目标
          CONFIG_IPV6=y          # 启用IPv6协议
          CONFIG_IP_SET=y          # 启用IP sets，用于管理IP地址集合
          CONFIG_IP_SET_BITMAP_IP=y          # 启用bitmap:ip类型的IP set
          CONFIG_IP_SET_BITMAP_IPMAC=y          # 启用bitmap:ip,mac类型的IP set
          CONFIG_IP_SET_BITMAP_PORT=y          # 启用bitmap:port类型的IP set
          CONFIG_IP_SET_HASH_IP=y          # 启用hash:ip类型的IP set
          CONFIG_IP_SET_HASH_IPMAC=y          # 启用hash:ip,mac类型的IP set
          CONFIG_IP_SET_HASH_IPMARK=y          # 启用hash:ip,mark类型的IP set
          CONFIG_IP_SET_HASH_IPPORT=y          # 启用hash:ip,port类型的IP set
          CONFIG_IP_SET_HASH_IPPORTIP=y          # 启用hash:ip,port,ip类型的IP set
          CONFIG_IP_SET_HASH_IPPORTNET=y          # 启用hash:ip,port,net类型的IP set
          CONFIG_IP_SET_HASH_MAC=y          # 启用hash:mac类型的IP set
          CONFIG_IP_SET_HASH_NET=y          # 启用hash:net类型的IP set
          CONFIG_IP_SET_HASH_NETIFACE=y          # 启用hash:net,iface类型的IP set
          CONFIG_IP_SET_HASH_NETNET=y          # 启用hash:net,net类型的IP set
          CONFIG_IP_SET_HASH_NETPORT=y          # 启用hash:net,port类型的IP set
          CONFIG_IP_SET_HASH_NETPORTNET=y          # 启用hash:net,port,net类型的IP set
          CONFIG_IP_SET_LIST_SET=y          # 启用list:set类型的IP set
          CONFIG_NET=y          # 启用网络支持
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y          # 启用addrtype netfilter匹配模块
          CONFIG_NETFILTER_XT_SET=y          # 启用set netfilter匹配模块
          CONFIG_NET_SCHED=y          # 启用网络数据包调度器
          CONFIG_NET_SCH_CAKE=y          # 启用CAKE（Common Applications Kept Enhanced）队列管理算法
          CONFIG_NET_SCH_FQ=y          # 启用Fair Queue（公平队列）调度器
          CONFIG_NET_SCH_FQ_CODEL=y          # 启用带CoDel的Fair Queue调度器
          CONFIG_TCP_CONG_ADVANCED=y          # 启用高级TCP拥塞控制算法
          CONFIG_TCP_CONG_BBR=y          # 启用BBR TCP拥塞控制算法
          CONFIG_TCP_CONG_BRUTAL=y          # 启用BRUTAL TCP拥塞控制算法
          CONFIG_TCP_CONG_CUBIC=y          # 启用CUBIC TCP拥塞控制算法
          CONFIG_TCP_CONG_HTCP=y          # 启用H-TCP拥塞控制算法
          CONFIG_TCP_CONG_NV=y          # 启用NV TCP拥塞控制算法
          CONFIG_TCP_CONG_VEGAS=y          # 启用Vegas TCP拥塞控制算法
          CONFIG_TCP_CONG_WESTWOOD=y          # 启用Westwood TCP拥塞控制算法
          CONFIG_TCP_FASTOPEN=y          # 启用TCP Fast Open
          CONFIG_TCP_SACK=y          # 启用TCP选择性确认（SACK）
          CONFIG_TCP_TIMESTAMPS=y          # 启用TCP时间戳

          #### 加密与压缩

          CONFIG_CRYPTO_842=y          # 启用842压缩算法的加密API
          CONFIG_CRYPTO_LZ4=y          # 启用LZ4压缩算法的加密API
          CONFIG_CRYPTO_LZ4HC=y          # 启用LZ4HC（高压缩）算法的加密API
          CONFIG_CRYPTO_LZ4K=y          # 启用LZ4K压缩算法的加密API
          CONFIG_CRYPTO_LZ4KD=y          # 启用LZ4KD解压算法的加密API

          #### IO和文件系统

          CONFIG_BFQ_ALLOC_BATCH=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_BLK_CGROUP_IOCOST=y
          CONFIG_BLK_MQ=y
          CONFIG_BLK_MQ_PCI=y
          CONFIG_BLK_WBT=y
          CONFIG_F2FS_FS_LZ4=y
          CONFIG_F2FS_FS_LZ4HC=y
          CONFIG_F2FS_FS_ZSTD=y
          CONFIG_IO_URING=y
          CONFIG_IO_URING_ASYNC=y
          CONFIG_MQ_IOSCHED_ADIOS=y          # 启用ADIOS I/O调度器
          CONFIG_MQ_IOSCHED_BFQ=y
          CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y          # 将ADIOS设置为默认I/O调度器
          CONFIG_PAGE_BOOST=y
          CONFIG_SWAP_ASYNC_IO=y
          CONFIG_TMPFS_POSIX_ACL=y          # 为tmpfs启用POSIX访问控制列表
          CONFIG_TMPFS_XATTR=y          # 为tmpfs启用扩展属性

          #### 特定硬件和驱动

          CONFIG_ARM64_SME=y          # 建议根据硬件支持和需求考虑启用，如果硬件不支持或不需要，可设置为n
          CONFIG_BT_QCA=y
          CONFIG_NVME_MULTIPATH=y
          CONFIG_NVME_TARGET=y

          #附加
          CONFIG_REKERNEL=y

          EOF
          
          sed -i 's/check_defconfig//' ./common/build.config.gki
          git add -A && git commit -a -m "BUILD Kernel with optimizations" || true
          echo "✅ 内核优化配置已完成"

      - name: Add make name
        run: |
         cd kernel_workspace/kernel_platform
         sed -i '208s|echo ".*"|echo "${KERNELVERSION}${scm_version}${config_localversion}"|' common/scripts/setlocalversion
         sed -i "s/\${scm_version}/${{ github.event.inputs.KERNEL_NAME }}/g" ./common/scripts/setlocalversion
         
      - name: 设置 CONFIG_LOCALVERSION
        run: |
         cd kernel_workspace/kernel_platform
         echo 'CONFIG_LOCALVERSION="${{ github.event.inputs.local_version }}"' >> ./common/arch/arm64/configs/gki_defconfig

      - name: 删除 CONFIG_LOCALVERSION 中的 -4k 后缀（如果勾选）
        if: ${{ fromJSON(github.event.inputs.remove_default_4k) }}
        run: |
         cd kernel_workspace/kernel_platform
         sed -i 's/-4k//' ./common/arch/arm64/configs/gki_defconfig
    
      - name: 拉取并集成 sched_ext 调度器
        run: |
         set -e
         cd kernel_workspace/kernel_platform/common
         git clone https://github.com/Numbersf/SCHED_PATCH.git -b "${{ github.event.inputs.CPU }}"
         echo "正在拉取风驰补丁"
         cp "./SCHED_PATCH/fengchi_${{ github.event.inputs.FEIL }}.patch" ./
         if [[ -f "fengchi_${{ github.event.inputs.FEIL }}.patch" ]]; then
         echo "正在打风驰补丁"
         dos2unix "fengchi_${{ github.event.inputs.FEIL }}.patch"
         patch -p1 -F 3 < "fengchi_${{ github.event.inputs.FEIL }}.patch"
         echo "✅ fengchi_patch 完成"
         else
         echo "❌ 未匹配到补丁，风驰内核暂未支持你的机型，请关闭后重试"
         exit 11
         fi

      - name: 拉取afdo
        run: |
          cd kernel_workspace/kernel_platform/
          git clone https://github.com/PxxPei/afdo.git

      - name: Set build timestamp
        run: |         
         export SOURCE_DATE_EPOCH=$(date -d "Tue Jul  1 19:48:18 UTC 2025" +%s)
         echo "SOURCE_DATE_EPOCH=$SOURCE_DATE_EPOCH" >> $GITHUB_ENV
   
      - name: Build Kernel
        run: |
         # 加入 clang 到 PATH
         export PATH="${{ github.workspace }}/kernel_workspace/kernel_platform/prebuilts/clang/host/linux-x86/clang-r510928/bin:$PATH"
         # 加入 ccache 到 PATH
         export PATH="/usr/lib/ccache:$PATH" 

         # 设置固定内核构建时间
         export KERNEL_TIME="Tue Jul  1 19:48:18 UTC 2025"
         export KBUILD_BUILD_TIMESTAMP="$KERNEL_TIME"
         export SOURCE_DATE_EPOCH=$(date -d "$KERNEL_TIME" +%s)
         # 设置 AFDO 配置文件路径
         export AFDO_PROFILE="${{ github.workspace }}/kernel_workspace/kernel_platform/afdo/kernel.afdo"
         if [ ! -f "$AFDO_PROFILE" ]; then
           echo "Error: AFDO profile not found at $AFDO_PROFILE"
         exit 1
         fi

         # 打印验证
         echo "KERNEL_TIME = $KERNEL_TIME"
         echo "SOURCE_DATE_EPOCH = $SOURCE_DATE_EPOCH"
         echo "Using KCFLAGS: -Wno-error -fauto-profile=$AFDO_PROFILE"

         # 设置一些通用参数（便于后续维护）
         export KCFLAGS="-Wno-error -fauto-profile=$AFDO_PROFILE"
         export MAKE_ARGS="LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang \
         RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc \
         PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole \
         LD=ld.lld HOSTLD=ld.lld O=out SOURCE_DATE_EPOCH=${SOURCE_DATE_EPOCH}"

         # 进入源码目录
         cd kernel_workspace/kernel_platform/common
         rm -rf out

         # 执行 defconfig
         export KBUILD_CFLAGS="-fpolly -flto=thin"
         make -j$(nproc) $MAKE_ARGS gki_defconfig

         # 编译全部目标
         make -j$(nproc) $MAKE_ARGS all

         # 创建输出目录并复制 Image
         mkdir -p ../dist
         cp out/arch/arm64/boot/Image ../dist/
         
      - name: 验证内核构建时间
        run: |
         strings kernel_workspace/kernel_platform/common/out/vmlinux | grep "UTC"

      - name: Prepare AnyKernel3 package
        run: |
          cd kernel_workspace/kernel_platform/dist
          git clone https://github.com/3620603660/AnyKernel3.git AnyKernel3 --depth=1
          rm -rf AnyKernel3/.git
          rm -f AnyKernel3/push.sh
          cp Image AnyKernel3/

      - name: Upload AnyKernel3 artifact
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3_oneplus_ace5pro_${{ env.KSUVER }}_ksun
          path: kernel_workspace/kernel_platform/dist/AnyKernel3/*
